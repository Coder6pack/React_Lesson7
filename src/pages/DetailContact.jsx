import React from "react";
import { Router, Routes, useParams } from "react-router-dom";

export default function DetailContact() {
  const param = useParams();
  return (
    <div>
      <h1>This is DeltailContact</h1>
    </div>
  );
}
